import 'package:flutter/material.dart';

/// Estructura fase 1: sin pantalla final. Texto simulado únicamente.
class HomeFeatureShell extends StatelessWidget {
  const HomeFeatureShell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Home · mock')),
    );
  }
}
