import 'package:flutter/material.dart';

class CalendarFeatureShell extends StatelessWidget {
  const CalendarFeatureShell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Calendario · mock (sin calendario real)')),
    );
  }
}
