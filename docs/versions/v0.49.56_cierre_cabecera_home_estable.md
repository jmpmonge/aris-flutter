# v0.49.56 — Cierre cabecera Home estable

## Objetivo del bloque

Cerrar definitivamente la cabecera viva de Home: saludo temporal, clima compacto, transición fluida y espaciado equilibrado con la tarjeta HOY.

## Resumen de cambios (v0.49.34 → v0.49.56)

| Área | Resultado |
|------|-----------|
| Saludo temporal | 7 s, fade in ~300 ms, sesión sin repetición |
| Colapso | 600 ms, `easeInOutCubic`, progreso único `collapseT` |
| Rebotes | Eliminados (sin bounce, settle, overshoot, doble fase) |
| Clima | Bloque único en Stack; escala 1.0→0.78; sin animación del sol |
| Tabulado | `homeHeaderHorizontalInset` simétrico (margen óptico intermedio) |
| Posición clima compacto | `_kWeatherCompactTopOffset = -12` |
| Padding inferior cabecera | `homeLiveHeaderBottomWithGreeting/Compact = 16` |
| HOY | Sincronizado vía `HomeGreetingSession.collapseProgress` |

## Archivos principales

- `lib/features/home/presentation/widgets/home_live_header.dart`
- `lib/features/home/presentation/widgets/home_weather_block.dart`
- `lib/features/home/presentation/widgets/home_greeting_session.dart`
- `lib/features/home/presentation/home_screen.dart`
- `lib/theme/app_spacing.dart`

## Pruebas ejecutadas

```bash
flutter clean
flutter pub get
flutter analyze
flutter run -d chrome
```

Checklist: saludo 7 s, clima compacto arriba, sin rebote ni pulso del sol, HOY con espacio correcto, input y botonera OK.

## Estado final

Cabecera estable y premium. Bloque cerrado.

**Siguiente bloque:** Calendario.
