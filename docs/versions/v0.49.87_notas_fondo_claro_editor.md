# v0.49.87 — Fondo claro en Notas y editor

## Objetivo

Corregir el fondo oscuro residual en la pestaña Notas y en el lienzo/editor de nota amplia en modo claro.

## Cambios

- `notes_screen.dart`: `ColoredBox` con `context.arisList.canvas` (#F5F7FB en claro).
- `manual_note_canvas_sheet.dart`: scaffold, textos, snackbars, hojas modales y barra superior vía `ArisListPalette`.
- Widgets del editor: `note_prose_block_field`, `note_checklist_line`, `note_table_block_editor`, `note_wide_editor_toolbar`.

## Modo oscuro

Sin cambios: `ArisListPalette.dark` conserva los tokens `noteWide*`.

## Validación

```text
flutter analyze — No issues found!
```
